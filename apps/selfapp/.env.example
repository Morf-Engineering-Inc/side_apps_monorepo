# Cognito Configuration
# For production deployment with AWS Cognito hosted UI
VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_COGNITO_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com

# Local Development Testing
# To test Cognito callback locally:
# 1. Copy this file to .env.local
# 2. Add your actual Cognito configuration values above
# 3. In your Cognito User Pool app client settings, add http://localhost:3003/callback to "Allowed callback URLs"
# 4. Start the dev server: npm run dev
# 5. The callback will work at http://localhost:3003/callback
#
# Note: The redirect URI is automatically set to http://localhost:3003/callback in development mode.
# You can override it by uncommenting and setting the variable below:
# VITE_COGNITO_REDIRECT_URI=http://localhost:3003/callback

# API Configuration (optional)
VITE_API_GATEWAY_URL=https://your-api-gateway-url.execute-api.us-east-1.amazonaws.com

# AWS Configuration (for backend services if needed)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_ACCOUNT_ID=
